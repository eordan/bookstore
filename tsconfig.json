{
  "compilerOptions": {
    "moduleResolution": "node",
    "esModuleInterop": true,
    "outDir": "./dist/",
    "strict": true,
    "noImplicitAny": true,
    "module": "es6",
    "target": "es5",
    "jsx": "react",
    "lib": ["esnext", "dom"],
    "allowJs": true,
    "baseUrl": "./",
    "paths": {
      "@components/*": ["src/components/*"]
    },
  },
  "include": ["./", "./.eslintrc.js"],
}

//Unfortunately I couldn't reproduce the error and after checking out the `develop` branch I saw no issues in my VSCode. But after examining possible reasons why this error could occur I found that it might be due to the fact that I had excluded 'node_modules' folder in `tsconfig.json`